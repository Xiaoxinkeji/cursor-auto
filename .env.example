# Cursor Pro 配置示例文件
# 复制此文件为 .env 并根据实际情况修改

# 基础配置
DOMAIN='xiao89.site'  # 您的域名或直接使用 gmail.com

# IMAP邮箱配置 (Gmail示例)
IMAP_SERVER=imap.gmail.com
IMAP_PORT=993
IMAP_USER=your-email@gmail.com
IMAP_PASS=your-app-password  # Gmail需要使用应用专用密码: https://myaccount.google.com/apppasswords
IMAP_DIR=INBOX
IMAP_PROTOCOL=IMAP  # 使用 IMAP 协议

# 浏览器配置
# 无头模式 (默认开启)
BROWSER_HEADLESS='True'

# 代理配置 (如果需要)
# BROWSER_PROXY='http://user:pass@host:port'

# 调试级别 (INFO, DEBUG, WARNING, ERROR)
# DEBUG_LEVEL=INFO

BROWSER_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/130.0.6723.92 Safari/537.36

# 使用其他浏览器(如Edge)
# BROWSER_PATH='C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe'

